{% extends 'base.html.twig' %}
{% block body %}
    <img src="{{ asset('images/index/gallery/' ~ image.file) }}" alt="{{ image.file }}" style="height: 100px;" class="img-fluid d-block mx-auto w-50">
    <p>{{image.name}}</p>
    <p>Likes {{image.likes}}</p>
    <p><a><button>Commentarios</button></a> {{image.numComments}}</p>
{{ include ('page/form_comment.html.twig') }}
{% endblock %}